batch_size: 8
best_model_path: artifacts/checkpoints_nlp/best_model.pth
checkpoint_dir: artifacts/checkpoints_nlp
disease_mapping_file: data/processed/disease_mapping.json
dropout: 0.2
early_stopping_min_delta: 0.001
early_stopping_patience: 3
epochs: 10
gamma: 0.1
learning_rate: 2.0e-05
max_grad_norm: 1.0
max_length: 64
mlflow_experiment_name: distilbert-symptom-classifier
mlflow_tracking_uri: sqlite:///mlflow.db
model_name: distilbert-base-uncased
optimizer: adamw
random_state: 42
scheduler: linear_warmup
step_size: 3
t_max: 5
test_split: 0.15
tokenizer_name: distilbert-base-uncased
train_split: 0.7
tune_batch_sizes:
- 8
- 16
tune_learning_rates:
- 1.0e-05
- 2.0e-05
- 3.0e-05
tuning_epochs: 2
use_class_weights: true
val_split: 0.15
validated_symptoms_csv: data/processed/disease_symptom_cleaned.csv
warmup_ratio: 0.1
weight_decay: 0.01
